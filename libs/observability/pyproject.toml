[project]
name = "syltra-observability"
version = "0.1.0"
description = "SYLTRA structured JSON logging, secret redaction, correlation IDs"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/syltra_observability"]
